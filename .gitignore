*.idea
node_modules
*.png
*.jpeg
*.jpg
*.DS_Store

# exclude *.csv
*.csv
*.xlsx

# exclude *.json
*.json

# exclude *.mp3
*.mp3

# exclude *.otf
*.otf

# exclude *.ttf
*.ttf

# exclude *.webp
*.webp

# exclude *.zip
*.zip

# 빌드 결과물 — webpack 이 소스를 합쳐 만든다
#
# 따라다니지 않는다. 소스에서 언제든 다시 만들 수 있고, 한 줄만 고쳐도 1MB 파일이
# 통째로 달라진 것으로 기록되어 커밋 기록에서 진짜 바뀐 소스가 안 보인다.
# 받아서 처음 여는 사람은 resource.zip 을 풀듯이 npm run <화면> 을 한 번 돌리면 된다.
dist/
test/dist/
