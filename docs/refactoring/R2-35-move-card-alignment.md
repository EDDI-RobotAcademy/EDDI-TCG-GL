# [ETWGL-R2-35] 카드 정렬 패키지 조정

선행: R2-34 │ 후행: R2-36 │ 산출물: src/battle/card/alignment/ │ Type: Structure │ Domain: Battle Card

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 카드를 제자리에 놓는 파일이 최상위에 혼자 있었다
        - R2-20 에서 [손패와 필드 양쪽을 정렬하니 갈라야 한다] 고 봤는데, 다시 재보니 가를 것이 아니었다
    - 목적
        - 카드를 제자리에 놓는 일을 카드 묶음 아래로 옮긴다
    - 기대 결과
        - 카드가 어디에 놓이는지 고칠 때 battle/card/ 한 묶음을 본다
    - 옮기기 전에 확인한 것
        - 밖에서 부르는 것은 손패 정렬 하나뿐이다. 일곱 번 불린다
        - 안에 두 갈래가 있다. 손패 카드를 페이지에 맞춰 놓는 것과, 필드에 나온 카드의 표시를 놓는 것
        - 두 갈래가 쓰는 저장소가 전부 battle/card/ 아래 것이다. 필드 쪽조차 필드 저장소가 아니라 카드 자리 저장소를 쓴다
    - 옮기는 것
        - 1파일 384줄
    - 건드리지 않는 것
        - 파일을 가르는 일 - 갈 곳이 같아서 가를 이유가 없다
        - 파일 안의 코드 - 위치와 경로만 바꾼다

2. 가를 것인지 옮길 것인지 다시 판단한다
    - R2-20 의 판단을 그대로 믿지 않고 코드로 확인한다
    - 두 갈래가 쓰는 저장소를 세어 어디에 속하는지 본다

3. 옮긴다
    - battle/card/alignment/ 로 둔다. 카드에 붙는 것들과 나란히 놓인다
    - 부르는 곳의 경로를 고친다

4. 이관이 성립하는지 확인한다
    - 타입 검사와 의존 검사가 통과한다
    - 빈 폴더와 나갔다 들어오는 경로를 본다

5. 게임 동작이 달라지지 않았는지 확인한다
    - 손패 카드가 줄지어 놓이는지
    - 손패 페이지를 넘길 때 다시 줄지어 놓이는지
    - 카드를 필드로 내보낼 때 표시가 자리에 붙는지
    - 네온 테두리가 카드를 따라오는지

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 가를 것이 아니라는 판단 기록

2. 가를지 옮길지 판단한다
    - [x] 밖에서 부르는 것 세기
    - [x] 두 갈래가 쓰는 저장소 확인
    - [x] R2-20 의 판단이 왜 달랐는지 확인

3. 옮긴다
    - [x] battle/card/alignment/ 로 1파일
    - [x] 부르는 곳 경로 수정
    - [x] 빈 폴더 삭제

4. 이관 확인
    - [x] npx tsc --noEmit 통과
    - [x] npm run depcruise 통과
    - [x] 나갔다 들어오는 경로 확인
    - [x] 시나리오 빌드 통과

5. 동작 확인
    - [x] 손패 정렬, 페이지 넘김, 필드 표시, 네온 테두리

# Issue

1. 가를 것인 줄 알았는데 옮길 것이었다
    - 증상
        - R2-20 에서 [손패와 필드 양쪽을 정렬하므로 어느 쪽으로 보내도 절반이 틀린다] 고 판단해 미뤄뒀다
        - 다시 재보니 두 갈래가 쓰는 저장소가 전부 카드 묶음 아래 것이었다
    - 원인
        - 그때는 battle/card/ 가 없었다. 카드에 붙는 것들이 최상위에 흩어져 있어서, 손패와 필드 사이에 걸친 것으로 보였다
        - 갈 곳이 없으면 걸쳐 있는 것처럼 보인다
    - 처리
        - 가르지 않고 옮겼다. 가르면 두 갈래가 함께 쓰는 부분을 또 어디 둘지 정해야 한다
        - **[걸쳐 있다] 는 판단은 갈 곳이 생긴 뒤에 다시 잰다.** 갈 곳이 없을 때는 다 걸쳐 보인다

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 옮긴 파일 | 1개 384줄 | 코드는 그대로다 |
| 가른 파일 | 0개 | 가를 이유가 없었다 |
| 고친 참조 | 4파일 | |
| 없어진 최상위 폴더 | 1개 | 205개 → 204개 |
| 타입 검사 | 오류 0 | |
| 의존 검사 | error 0, warn 6 | 기존 순환 그대로 |
| 동작 변경 | 없음 | 위치와 경로만 바꿨다 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | 앞선 판단을 그대로 따르지 않고 코드로 다시 쟀다. 계획대로 갈랐으면 함께 쓰는 부분을 어디 둘지 또 정해야 했다. |
| **잘 맞은 것** | R2-34 를 먼저 해서 갈 곳이 생겼다. 순서가 반대였으면 이번에도 걸쳐 있는 것으로 보였을 것이다. |
| **미흡한 것** | 이 파일은 저장소 열둘을 끌어쓴다. 옮기기만 해서 그건 그대로다. |
| **다음 작업에 넘길 것** | 저장소 열둘을 끌어쓰는 것. 무엇이 왜 필요한지 따져 줄일 수 있는지 본다 |
