# [ETWGL-R2-8] 무덤과 로스트존 화면을 바꿀 때 한 곳만 고치도록 두 개의 그리는 작업을 하나로 통합한다

선행: R2-7 │ 후행: 없음 │ 상태: **폐기** — 착수 전에 전제가 무너졌다 │ Type: Structure │ Domain: Battle

> 이 작업은 하지 않는다. 왜 하지 않기로 했는지는 Issue 와 Review 에 있다.
> 문서를 지우지 않는 이유는, 지우면 다음에 같은 판단을 처음부터 다시 하게 되기 때문이다.

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 무덤을 그리는 작업이 내 것과 상대 것으로 두 개다. 이름을 빼면 29줄 차이다
        - 로스트존을 그리는 작업도 두 개다. 이름을 빼면 8줄 차이다
        - 무덤 그리는 방식을 바꾸려면 두 파일을 고쳐야 한다
        - 카드 격자 팝업은 이미 하나를 다섯 곳이 배치 값만 바꿔 부르고 있다. 패널도 같은 방식이 가능해 보인다
    - 목적
        - 그리는 작업을 하나로 통합하고 배치 값만 각자 넘긴다
    - 기대 결과
        - 무덤 그리는 방식을 바꿀 때 한 파일만 고친다
    - 건드리는 것
        - 무덤 패널 렌더러 2개를 1개로, 로스트존 패널 렌더러 2개를 1개로
        - 시나리오의 호출부
    - 건드리지 않는 것
        - 화면 배치 값 4개 - 내 것과 상대 것이 실제로 다르다 (R2-7 판정)
        - 저장소와 엔티티 - R2-12 에서 전투 상태로 흡수된다

2. 무덤을 그리는 작업을 하나로 통합한다
3. 로스트존을 그리는 작업을 하나로 통합한다
4. 시나리오가 배치 값만 바꿔 부르게 한다
5. 그리는 결과가 이전과 같은지 확인한다

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 배치 값과 저장소를 범위 밖으로 명시

2. 착수 전 전제 확인
    - [x] 내 무덤과 상대 무덤이 앞으로도 같은 이유로 바뀌는지 확인
    - [x] 확인 결과 전제가 무너져 이후 작업을 중단

3. 무덤을 그리는 작업을 하나로 통합한다
    - [ ] 하지 않는다

4. 로스트존을 그리는 작업을 하나로 통합한다
    - [ ] 하지 않는다

5. 폐기 근거를 남긴다
    - [x] 왜 하지 않는지를 Issue 와 Review 에 기록
    - [x] docs/merge_or_split.md 의 판정을 [합치지 않는다] 로 정정
    - [x] INDEX.md 에 폐기 표시

# Issue

1. 착수 전에 전제가 무너졌다
    - 증상
        - 그리는 코드가 이름을 빼면 8줄, 29줄 차이라 합칠 수 있다고 판단했다
        - 그런데 앞으로 부활 관련 덱이 들어오면 내 무덤만 다른 화면이 된다
    - 원인
        - R2-7 에서 판정할 때 카드 데이터만 봤다. 지금 카드 100장에 부활 카드가 0장이라 근거를 찾지 못했다
        - **데이터에 없는 요구는 데이터에서 찾을 수 없다.** 만드는 사람에게 물었어야 했다
    - 처리
        - 작업을 착수하지 않고 폐기했다
        - docs/merge_or_split.md 에 부활 근거를 넣고 판정을 [합치지 않는다] 로 바꿨다
        - 판단 근거를 찾는 곳에 [만드는 사람이 알고 있는 계획] 을 추가했다

2. R2-7 의 판정이 절반만 맞았다
    - 증상
        - R2-7 은 배치 값은 나누고 그리는 코드는 합치라고 판정했다
        - 배치 값 판정은 맞았지만 그리는 코드 판정은 틀렸다
    - 원인
        - 배치 값은 지금 코드에서 차이가 보였다. 이름을 빼도 83줄, 48줄 달랐다
        - 그리는 코드는 지금 차이가 없어서 앞으로도 없을 것이라 봤다.
          앞으로 무엇이 붙을지는 코드에 안 적혀 있다
    - 처리
        - R2-7 의 판정 자체는 남긴다. 무엇을 보고 판단했고 무엇을 놓쳤는지가 기록이다
        - merge_or_split.md 의 표에 정정과 사유를 함께 적었다

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 착수 여부 | 하지 않음 | 전제가 무너져 코드를 건드리기 전에 멈췄다 |
| 소스 코드 변경 | 0줄 | |
| 합치려던 파일 | 4개 (무덤 2, 로스트존 2) | 그대로 둔다 |
| 지금 카드 데이터의 부활 카드 | 0장 | **이것이 넷을 비슷해 보이게 만든 원인이다** |
| 앞으로 들어올 덱 | 언데드 사령, 휴먼 소생, 천사 부활 | 셋 다 무덤을 대상으로 삼는다 |
| 부활 이후 갈라지는 것 | 내 무덤 하나 | 나머지 셋은 보여주기만 한다 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | 코드를 건드리기 전에 멈췄다. 합친 뒤에 알았으면 되돌리는 비용이 들었을 것이다. 백로그를 먼저 쓰고 확인하는 순서가 여기서 값을 했다. |
| **미흡한 것** | R2-7 에서 판단 근거를 카드 데이터에서만 찾았다. 부활 카드가 0장이라 근거가 안 나왔고 [지금 같으니 앞으로도 같겠다] 로 넘어갔다. **데이터에 아직 없는 계획은 만드는 사람에게 물어야 한다.** |
| **미흡한 것** | 이 작업이 두 가지를 묶고 있었다. 그리는 코드를 합치는 일과 카드 격자 팝업 이름을 고치는 일은 다른 이유로 생긴 문제다. 규칙 14 를 만들어놓고 지키지 않았다. |
| **다음 작업에 넘길 것** | 카드 격자 팝업의 이름과 자리 문제. 이건 폐기 사유와 무관하게 여전히 남아 있다. `YourLostZonePopupRendererV2` 가 레오닉 카드 선택에도 쓰인다. |
