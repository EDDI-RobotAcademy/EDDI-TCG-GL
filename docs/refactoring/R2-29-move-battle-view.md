# [ETWGL-R2-29] 전투 화면 조립 패키지 조정

선행: R2-19 │ 후행: R2-30 │ 산출물: src/battle/view/ │ Type: Structure │ Domain: Battle

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 전투 화면을 조립하는 파일이 src/ui/screens/ 아래에 혼자 있었다. 전투에 속한 것이 전투 밖에 있다
        - 그 폴더는 화면 넷 중 둘만 옮겨둔 상태라 자리를 잡은 구조가 아니다. 로비, 상점, 보유 카드는 최상위에 있다
    - 목적
        - 전투 화면 조립을 전투 아래로 모은다
    - 기대 결과
        - 전투 화면을 고칠 때 battle/ 안에서 찾는다
    - 옮기기 전에 확인한 것
        - 이 파일은 전투 화면을 조립한다. 화면과 렌더러와 카메라를 만들고, 손패와 양쪽 필드를 붙이고, 마우스와 키보드를 잇고, 필드 에너지와 턴 표시를 띄운다
        - 라우터에 등록된 화면은 다른 파일이다. 그쪽은 배경 한 장만 그린다
        - 둘은 복사본이 아니다. 하나는 진입점이고 하나는 조립이다
    - 옮기는 것
        - 1파일 285줄
    - 건드리지 않는 것
        - 라우터에 등록된 진입점 - 로비, 상점, 보유 카드와 같은 갈래다. 그 넷을 함께 정리할 때 본다
        - 진입점이 실제 전투 화면을 띄우게 잇는 일 - 동작이 크게 달라진다. R2-58
        - 덱 편성 화면 - 전투가 아니다

2. 진입점과 조립을 왜 나누는지 정한다
    - 앞으로 전투 화면을 쓰는 곳이 둘이 된다. 상대와 하는 대전, 그리고 신규 카드를 시험해 보는 시뮬레이션
    - 둘 다 같은 전투 화면을 쓰고 진입점만 다르다
    - 조립을 진입점 안에 넣으면, 한쪽을 접을 때 그 안의 조립까지 딸려 나가 다시 꺼내야 한다
    - 나눠 두면 접을 때 진입점 하나만 지우면 된다

3. 옮기고 경로를 잇는다
    - 경로는 풀어서 다시 계산한다
    - 빈 폴더가 남았는지 본다

4. 이관이 성립하는지 확인한다
    - 타입 검사와 의존 검사가 통과한다
    - 나갔다 들어오는 경로가 생겼는지 본다

5. 게임 동작이 달라지지 않았는지 확인한다
    - 이 파일을 쓰는 시나리오가 그대로 도는지 본다

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 진입점을 범위 밖으로 명시

2. 진입점과 조립의 관계를 정한다
    - [x] 앞으로 모드가 둘이 되는 것 확인
    - [x] 한쪽을 접을 때 어떻게 되는지 확인

3. 옮긴다
    - [x] battle/view/ 로 1파일
    - [x] 부르는 곳 경로 수정
    - [x] 빈 폴더 삭제

4. 이관 확인
    - [x] npx tsc --noEmit 통과
    - [x] npm run depcruise 통과
    - [x] 나갔다 들어오는 경로 확인
    - [x] 시나리오 빌드 통과

5. 동작 확인
    - [x] draw-entity-frame-renderer 시나리오가 그대로 도는지 확인

# Issue

1. 두 화면이 복사본인 줄 알았다
    - 증상
        - R2-19 조사에서 [전투 화면과 시뮬레이션 전투 화면이 사실상 복사본] 이라고 봤다
        - 실제로 복사본이었던 것은 지금 지운 옛 화면과 진입점이었고, 남은 둘은 전혀 다른 것이었다
    - 원인
        - 이름이 셋 다 비슷해서 어느 둘을 비교했는지가 흐려졌다
        - TCGBattleFieldView, TCGSimulationBattleFieldView, BattleFieldView 가 함께 있었다
    - 처리
        - 남은 둘을 열어 확인했다. 137줄은 배경만 그리고 285줄은 전투를 조립한다
        - **비슷한 이름이 셋 있으면 어느 둘을 비교하는지 매번 적는다**

2. 라우터에 붙은 것이 껍데기다
    - 증상
        - 라우터가 부르는 화면은 배경 한 장만 그린다. 진짜 전투 화면은 어느 경로에도 안 붙어 있다
    - 원인
        - 전투를 시나리오로 만들면서 진입점을 잇지 않았다
    - 처리
        - 이번 범위 밖이다. 잇는 것은 동작이 크게 달라진다
        - R2-58 로 등록했다

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 옮긴 파일 | 1개 285줄 | 코드는 그대로다 |
| 고친 참조 | 2파일 | 자기 자신과 시나리오 하나 |
| 없어진 폴더 | src/ui/screens/battle_field | src/ui 에는 덱 편성 화면만 남았다 |
| 타입 검사 | 오류 0 | |
| 의존 검사 | error 0, warn 6 | 기존 순환 그대로 |
| 동작 변경 | 없음 | 위치와 경로만 바꿨다 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | 진입점을 함께 옮기지 않았다. 앞으로 대전과 시뮬레이션 두 진입점이 같은 전투 화면을 쓰게 되는데, 조립을 진입점 안에 넣었으면 한쪽을 접을 때 다시 꺼내야 한다. |
| **잘 맞은 것** | 두 화면이 복사본이라는 앞선 판단을 다시 확인했다. 그대로 갔으면 합치려 했을 것이다. |
| **미흡한 것** | 이름이 비슷한 셋을 비교하면서 어느 둘인지 흐려졌다. |
| **다음 작업에 넘길 것** | 진입점이 실제 전투 화면을 띄우게 잇는다. R2-58 |
| **다음 작업에 넘길 것** | 라우터에 붙은 화면 넷이 최상위에 흩어져 있다. 함께 정리할 때 본다 |
