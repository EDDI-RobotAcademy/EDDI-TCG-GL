# [ETWGL-R2-37] 본체 체력 패키지 조정

선행: 없음 │ 후행: R2-38 │ 산출물: src/battle/master_hp/ │ Type: Structure │ Domain: Battle

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 본체 체력 표기가 최상위에 남아 있었다. 전투에만 있는 것인데 전투 밖에 있다
        - R2-21 에서 유닛과 함께 옮기려다 [필드 위 유닛과 다른 것] 이라 빼둔 것이다
    - 목적
        - 본체 체력을 전투 아래로 옮긴다
    - 기대 결과
        - 전투 화면에 나오는 것이 전부 battle/ 안에 있다
    - 옮기기 전에 확인한 것
        - 내 본체와 상대 본체를 한 파일이 다룬다. 자리 값 함수가 둘이고 그리는 것은 하나다
        - 상대 자리는 내 자리를 뒤집은 값이라 함께 바뀐다. 그래서 가르지 않는다
        - 쓰는 곳은 시나리오 하나뿐이다
    - 옮기는 것
        - 2파일
    - 건드리지 않는 것
        - 내 쪽과 상대 쪽을 가르는 일 - 자리 값만 다르고 그리는 방식이 같다. 가를 이유가 없다
        - 파일 안의 코드 - 위치와 경로만 바꾼다

2. 내 쪽과 상대 쪽을 가를지 판단한다
    - 무덤이나 필드 에너지는 your, opponent 로 갈랐다. 여기도 갈라야 하는지 본다
    - 그리는 방식이 같고 자리 값만 다르면 가르지 않는다

3. 옮긴다
    - battle/master_hp/ 로 둔다
    - 부르는 곳의 경로를 고친다

4. 이관이 성립하는지 확인한다
    - 타입 검사와 의존 검사가 통과한다
    - 이미지 폴더 이름은 건드리지 않는다. 코드 경로와 글자가 겹친다
    - 빈 폴더와 나갔다 들어오는 경로를 본다

5. 게임 동작이 달라지지 않았는지 확인한다
    - 내 본체와 상대 본체의 체력이 화면에 보이는지
    - 본체를 공격했을 때 숫자가 줄어드는지

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 가르지 않는 사유 작성

2. 가를지 판단한다
    - [x] 자리 값 함수 둘과 그리는 것 하나 확인
    - [x] 상대 자리가 내 자리를 뒤집은 값인지 확인

3. 옮긴다
    - [x] battle/master_hp/ 로 2파일
    - [x] 부르는 곳 경로 수정
    - [x] 빈 폴더 삭제

4. 이관 확인
    - [x] npx tsc --noEmit 통과
    - [x] npm run depcruise 통과
    - [x] 이미지 폴더 이름을 안 건드렸는지 확인
    - [x] 시나리오 빌드 통과

5. 동작 확인
    - [x] 내 본체와 상대 본체의 체력 표기, 공격했을 때 줄어드는지

# Issue

1. 없음

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 옮긴 파일 | 2개 | 코드는 그대로다 |
| 가른 것 | 없음 | 그리는 방식이 같고 자리 값만 다르다 |
| 고친 참조 | 3파일 | 옮긴 파일 자신 포함 |
| 없어진 최상위 폴더 | 1개 | 204개 → 203개 |
| 타입 검사 | 오류 0 | |
| 의존 검사 | error 0, warn 6 | 기존 순환 그대로 |
| 동작 변경 | 없음 | 위치와 경로만 바꿨다 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | 무덤이나 필드 에너지처럼 your, opponent 로 가르지 않았다. 저쪽은 부활 규칙이 달라 갈랐지만, 본체 체력은 자리 값만 다르고 그리는 방식이 같다. |
| **잘 맞은 것** | 이미지 폴더 이름을 건드리지 않았다. 체력 그림이 resource/battle_field_unit/hp/ 에 있어 코드 경로와 글자가 겹친다. R2-21 에서 같은 함정을 만났다. |
| **미흡한 것** | 이 두 파일은 R2-21 에서 빼둔 뒤 열여섯 티켓 동안 남아 있었다. 선행이 없어 아무 때나 할 수 있었다. |
| **다음 작업에 넘길 것** | 본체 체력을 담는 값이 시나리오 안에 있다. 재접속하면 사라진다. R2-49 에서 본다 |
