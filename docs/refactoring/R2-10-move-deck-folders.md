# [ETWGL-R2-10] Your / Opponent Deck 패키지 조정

선행: R2-9 │ 후행: R2-11 │ 산출물: src/battle/zone/{your_deck, opponent_deck} │ Type: Structure │ Domain: Battle

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 전투 코드를 battle 아래로 모으는 중인데 덱 두 폴더가 아직 src 최상위에 있다
        - 무덤과 로스트존은 이미 battle/zone/ 으로 옮겼는데 덱만 남아 있다. 셋 다 카드가 머무는 곳이다
        - R2-6 에서 덱을 함께 옮기지 않은 것은 이관 위험이 달라서였지 자리가 달라서가 아니었다
    - 목적
        - 덱 두 폴더를 카드가 머무는 곳끼리 모아둔 자리로 옮긴다
    - 기대 결과
        - 카드가 어디에 있는지를 다룰 때 한 폴더 아래에서 덱, 무덤, 로스트존을 함께 본다
    - 왜 이 묶음이 먼저인가
        - 밖에서 이 둘을 참조하는 파일이 시나리오 하나뿐이다. 남은 37개 중 가장 적다
        - 목적지가 이미 있다. 무덤과 로스트존이 그 자리에 있다
    - 건드리는 것
        - your_deck, opponent_deck 두 폴더의 위치
        - 이 둘을 참조하는 시나리오의 경로
    - 건드리지 않는 것
        - 폴더 안의 코드 - 위치와 경로만 바꾼다
        - 저장소 - 담고 있는 카드 목록은 재접속 시 복원할 값이라 R2-19 에서 전투 상태로 흡수된다
        - your 와 opponent 중복 - 통합하지 않는다. 판단은 merge_or_split.md 를 따른다
        - 폴더 이름 - 옮기기만 한다

2. 두 폴더를 옮기고 끊어진 참조를 잇는다
    - R2-5 의 계획표에서 정한 자리로 옮긴다
    - 이력이 끊기지 않게 옮긴다
    - 시나리오의 참조 경로를 고친다

3. 이관이 성립하는지 확인한다
    - 타입 검사와 의존 검사가 통과한다
    - 옮긴 자리가 R2-3 의 의존 규칙을 어기지 않는다

4. 게임 동작이 달라지지 않았는지 확인한다
    - 개발 서버를 다시 켜고 확인한다. 파일을 옮기면 옛 번들을 보게 된다
    - 카드를 뽑는 동작과 덱에서 카드를 찾는 카드가 이전과 같이 도는지 본다

5. 다음 묶음에 넘길 것을 기록한다
    - 고친 참조 수와 걸린 시간을 기록한다
    - R2-5 의 계획표와 실제가 다르면 표를 고친다

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 저장소와 중복 통합을 범위 밖으로 명시

2. 두 폴더를 옮긴다
    - [x] git mv 로 이동
    - [x] 시나리오의 참조 경로 수정
    - [x] 폴더 안의 상대 경로가 있으면 수정

3. 이관이 성립하는지 확인한다
    - [x] npx tsc --noEmit 통과
    - [x] npm run depcruise 통과

4. 동작 확인
    - [x] 개발 서버 재시작 후 확인
    - [x] 드로우, 레오닉의 부름, 망자의 늪, 넘쳐흐르는 사기가 이전과 같이 도는지 확인

5. 커밋 후와 다음 묶음
    - [ ] git log --follow 로 이력 확인
    - [x] 고친 참조 수를 Review 에 기록
    - [x] 계획표와 실제가 다르면 수정

# Issue

없음. 계획표대로 진행됐고 예상과 다른 것이 나오지 않았다.

R2-6, R2-9 에서 겪었던 것들이 이번에는 나오지 않았다.
개발 서버를 미리 껐고, 옮길 자리가 R2-5 에서 정해져 있었고,
합칠지 나눌지는 merge_or_split.md 가 이미 답했다.

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 옮긴 폴더 | 2개 (your_deck, opponent_deck) | |
| 옮긴 파일 | 6개 | 각각 entity 1개와 repository 2개 |
| 고친 참조 | 2곳 (시나리오) | **예상대로 가장 적었다.** R2-6 은 24곳이었다 |
| 폴더 안에서 밖을 참조하는 곳 | 0곳 | 고칠 상대 경로가 없었다 |
| 타입 검사 | 오류 0 | |
| 의존 검사 | error 0, warn 6 | 기존 순환 그대로. 새 위반 없음 |
| 동작 확인 | 드로우, 레오닉의 부름, 망자의 늪, 넘쳐흐르는 사기 정상 | 덱을 건드리는 동작 넷 |
| 코드 내용 변경 | 0줄 | 위치와 경로만 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | R2-5 의 순서가 맞았다. 밖에서 참조하는 파일이 시나리오 하나뿐이라 고칠 곳이 2곳으로 끝났다. 참조 수로 순서를 정한 것이 실제로 통했다. |
| **잘 맞은 것** | 앞선 작업에서 겪은 것들이 되풀이되지 않았다. 개발 서버를 미리 껐고(R2-9), 자리가 정해져 있었고(R2-5), 통합 판단이 이미 있었다(R2-7). 앞 작업의 기록이 값을 했다. |
| **미흡한 것** | 이 작업에서 새로 배운 것이 없다. 계획표가 있으면 이런 이동은 기계적이다. 남은 일곱 묶음도 같을 것이므로, 매번 백로그를 다섯 흐름으로 쓰는 것이 과할 수 있다. |
| **다음 작업에 넘길 것** | R2-11(카드 능력 세 폴더)은 밖에서 참조하는 파일이 6개다. R2-5 계획표의 순서를 그대로 따른다. 이동 작업의 백로그를 줄일지는 R2-11 을 해보고 정한다. |
