# [ETWGL-R2-40] 유닛 카드 스킬 연출 패키지 조정

선행: R2-39 │ 후행: R2-41 │ 산출물: src/battle/animation/card/unit/019_nether_blade/skill/ │ Type: Structure │ Domain: Battle Animation

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 매 턴 도는 패시브 연출이 최상위 src/animation/ 아래에 남아 있었다
        - 같은 카드의 등장 연출은 R2-39 에서 옮겼는데 이쪽만 밖에 있어 한 카드가 두 곳에 걸쳐 있었다
    - 목적
        - 같은 카드의 연출을 한 자리에 모으되, 도는 시점이 다른 것은 나눠 둔다
    - 기대 결과
        - 카드 19번의 연출을 고칠 때 한 폴더를 열고, 그 안에서 출격 때 도는 것과 매 턴 도는 것이 구분된다
    - 옮기기 전에 확인한 것
        - 셋 중 둘은 첫 번째와 두 번째 패시브고, 하나는 그 둘이 도입부에 함께 쓰는 조각이다
        - 함께 쓰는 조각이 있으므로 셋을 같은 자리에 둔다
        - 부르는 곳은 배선 둘과 확인용 화면 하나다
    - 옮기는 것
        - 3파일 2,107줄
    - 건드리지 않는 것
        - 벨른의 광역기 - 파일이 아니라 공격 연출 파일 안의 메서드다. 꺼내는 것은 코드를 바꾸는 일이라 별도 작업으로 뺀다
        - 파일 안의 코드 - 위치와 경로만 바꾼다

2. 등장 연출과 왜 나누는지 확인한다
    - 등장은 출격할 때 한 번 돌고 다시 돌지 않는다
    - 패시브는 매 턴 돈다
    - 고칠 이유가 다르므로 한 폴더 안에서 나눈다

3. 옮긴다
    - 옮기기 전에 옛 자리와 새 자리를 짝지은 표를 만든다
    - 부르는 곳의 경로를 고친다

4. 이관이 성립하는지 확인한다
    - 타입 검사와 의존 검사가 통과한다
    - 빈 폴더와 나갔다 들어오는 경로를 본다

5. 게임 동작이 달라지지 않았는지 확인한다
    - 네더 블레이드의 첫 번째 스킬과 두 번째 스킬
    - 출격할 때 등장 연출에 이어 패시브가 도는지

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 벨른의 광역기를 범위 밖으로 명시

2. 나누는 이유를 확인한다
    - [x] 도는 시점이 다른 것 확인
    - [x] 셋이 서로 부르는지 확인

3. 옮긴다
    - [x] skill 아래로 3파일
    - [x] 부르는 곳 경로 수정
    - [x] 빈 폴더 삭제

4. 이관 확인
    - [x] npx tsc --noEmit 통과
    - [x] npm run depcruise 통과
    - [x] 나갔다 들어오는 경로 확인
    - [x] 시나리오 빌드 통과

5. 동작 확인
    - [x] 네더 블레이드 첫 번째 스킬, 두 번째 스킬, 출격 시 이어지는 패시브

# Issue

1. 제목에 있던 카드가 하나 빠졌다
    - 증상
        - 이 티켓의 원래 제목은 [유닛 카드 스킬 연출 패키지 조정 (19 네더 블레이드, 27 벨른)] 이었다
        - 실제로 옮긴 것은 네더 블레이드뿐이다
    - 원인
        - 벨른의 광역기는 파일로 있지 않고 공격 연출 파일 안의 메서드다
        - 계획할 때 카드번호로만 세고 그것이 파일인지 확인하지 않았다
    - 처리
        - 이번 범위에서 빼고 별도 작업으로 등록했다. 메서드를 꺼내는 것은 코드를 바꾸는 일이라 확인 범위가 달라진다
        - **카드 단위로 셀 때 그것이 파일인지 확인한다.** 카드 하나가 파일 하나인 것이 아니다

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 옮긴 파일 | 3개 2,107줄 | 코드는 그대로다 |
| 고친 참조 | 3파일 | 배선 둘과 확인용 화면 하나 |
| 없어진 최상위 폴더 | 1개 | src/animation/nether_blade |
| 타입 검사 | 오류 0 | |
| 의존 검사 | error 0, warn 6 | 기존 순환 그대로 |
| 동작 변경 | 없음 | 위치와 경로만 바꿨다 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | 등장과 스킬을 한 폴더 안에서 나눴다. 같은 카드지만 출격 때 한 번 도는 것과 매 턴 도는 것은 고칠 이유가 다르다. |
| **잘 맞은 것** | 함께 쓰는 조각을 같은 자리에 두었다. 두 패시브가 도입부에 같은 것을 쓰므로 갈라두면 그 조각을 또 어디 둘지 정해야 한다. |
| **미흡한 것** | 제목에 있던 벨른이 파일이 아니라 메서드였다. 계획할 때 확인하지 않았다. |
| **다음 작업에 넘길 것** | 벨른의 광역기를 공격 연출 파일에서 꺼낸다 |
