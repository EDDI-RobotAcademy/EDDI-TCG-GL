# [ETWGL-R2-33] 상대 필드 카드 표시 패키지 조정

선행: R2-22 │ 후행: R2-34 │ 산출물: src/battle/field/opponent/attribute_mark*/ │ Type: Structure │ Domain: Battle Field

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 상대 카드에 붙는 표시가 최상위에 세 폴더로 흩어져 있었다. 상대 필드는 이미 전투 아래로 옮겨졌는데 표시만 밖에 남았다
        - R2-21 에서 이 셋을 유닛에서 빼면서 자리가 정해질 때까지 미뤄둔 것들이다
    - 목적
        - 상대 필드에 붙는 표시를 상대 필드 아래로 모은다
    - 기대 결과
        - 상대 필드를 고칠 때 표시까지 한 묶음에서 찾는다
    - 옮기기 전에 확인한 것
        - 셋 다 상대 필드에만 붙는다. 상대 손패는 화면에 안 보이므로 손패 쪽 쓰임이 없다
        - 표시 종류를 담은 열거형이 내 쪽과 상대 쪽에 두 벌 있다. 값은 같고 쉼표 하나만 다르다
    - 옮기는 것
        - 11파일
    - 건드리지 않는 것
        - 두 벌인 표시 종류 열거형 - 합치는 것은 부르는 곳 열 곳을 고치는 일이다. R2-34 에서 내 쪽을 옮길 때 양쪽을 놓고 판단한다
        - 이름을 Cache, Store 로 바꾸는 일 - R2-26 에서 자리가 안 정해져 미뤄둔 것이다. 이제 자리가 잡혔으니 다음에 한다
        - 파일 안의 코드 - 위치와 경로만 바꾼다

2. 상대 쪽에만 붙는 것이 맞는지 확인한다
    - 부르는 곳을 세어 손패 쪽 쓰임이 있는지 본다
    - 내 쪽 표시와 어떻게 다른지 본다

3. 옮긴다
    - 옮기기 전에 옛 자리와 새 자리를 짝지은 표를 만든다
    - 이미 있는 card_position, card_scene 과 같은 모양으로 둔다

4. 이관이 성립하는지 확인한다
    - 타입 검사와 의존 검사가 통과한다
    - 빈 폴더와 나갔다 들어오는 경로를 본다

5. 게임 동작이 달라지지 않았는지 확인한다
    - 상대 필드 카드에 종족, 무기, 체력, 에너지 표시가 보이는지
    - 상대 카드를 공격하거나 스킬을 쓸 때 표시가 따라 움직이는지
    - 상대 카드 상세 보기와 네온 테두리

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 두 벌인 열거형과 개명을 범위 밖으로 명시

2. 상대 쪽 것이 맞는지 확인한다
    - [x] 부르는 곳 세기
    - [x] 내 쪽 열거형과 맞대어 보기

3. 옮긴다
    - [x] attribute_mark 4파일
    - [x] attribute_mark_position 3파일
    - [x] attribute_mark_scene 4파일
    - [x] 부르는 곳 경로 수정
    - [x] 빈 폴더 삭제

4. 이관 확인
    - [x] npx tsc --noEmit 통과
    - [x] npm run depcruise 통과
    - [x] 빈 폴더와 나갔다 들어오는 경로 확인
    - [x] 시나리오 빌드 통과

5. 동작 확인
    - [x] 상대 필드 카드의 표시와 공격, 스킬, 상세 보기, 네온 테두리

# Issue

1. 표시 종류가 두 벌인데 쉼표 하나만 다르다
    - 증상
        - MarkSceneType 이 내 쪽과 상대 쪽에 각각 있다
        - 값은 RACE, SWORD, STAFF, KINDS, ENERGY, ENERGY_COUNT, HP 로 같고, 마지막 줄 쉼표만 다르다
    - 원인
        - 상대 쪽을 만들면서 내 쪽을 베꼈다
    - 처리
        - 이번엔 옮기기만 했다. 합치면 부르는 곳 열 곳을 고쳐야 해 성격이 다르다
        - R2-34 에서 내 쪽 표시를 옮길 때 양쪽을 놓고 판단한다
        - **값이 같은 두 벌은 한쪽만 고치면 어긋난다.** 표시 종류를 하나 늘릴 때 두 곳을 고쳐야 한다

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 옮긴 파일 | 11개 | 코드는 그대로다 |
| 없어진 최상위 폴더 | 3개 | 상대 필드 표시가 전부 전투 아래로 들어왔다 |
| 고친 참조 | 12파일 | 옮긴 파일 자신 포함 |
| 타입 검사 | 오류 0 | |
| 의존 검사 | error 0, warn 6 | 기존 순환 그대로 |
| 동작 변경 | 없음 | 위치와 경로만 바꿨다 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | 옮기기 전에 옛 자리와 새 자리를 짝지은 표를 먼저 만들었다. R2-20 에서 옮긴 뒤에 훑다가 옛 위치를 잃어버려 두 번 훑었던 것을 되풀이하지 않았다. |
| **잘 맞은 것** | 두 벌인 열거형을 발견했지만 합치지 않았다. 옮기기와 합치기를 섞으면 화면이 어긋났을 때 원인을 가릴 수 없다. |
| **미흡한 것** | 이 셋은 R2-21 에서 이미 [자리가 정해지면 옮긴다] 고 미뤄둔 것이다. R2-22 가 끝난 시점에 바로 할 수 있었는데 열 티켓 뒤로 밀렸다. |
| **다음 작업에 넘길 것** | 두 벌인 표시 종류 열거형. R2-34 에서 양쪽을 놓고 판단한다 |
| **다음 작업에 넘길 것** | 이름을 Cache, Store 로 바꾸는 일. 자리가 잡혔으니 이제 할 수 있다 |
