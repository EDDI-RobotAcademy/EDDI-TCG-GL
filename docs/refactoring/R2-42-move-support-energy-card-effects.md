# [ETWGL-R2-42] 서포트, 에너지 카드 연출 패키지 조정

선행: R2-41 │ 후행: R2-43 │ 산출물: src/battle/animation/card/{support,energy}/ │ Type: Structure │ Domain: Battle Animation

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 서포트 세 장과 에너지 한 장의 연출이 최상위에 효과 이름으로 남아 있었다
        - 유닛과 아이템은 옮겼는데 이 넷만 밖에 있어 카드 연출이 두 곳에 걸쳐 있었다
    - 목적
        - 카드 연출을 종류별 서랍에 모두 넣어, 최상위에 카드 연출이 남지 않게 한다
    - 기대 결과
        - 카드 연출을 찾을 때 battle/animation/card/ 한 곳만 본다
    - 옮기기 전에 확인한 것
        - 넷의 종류를 카드 데이터에서 확인했다. 셋은 서포트, 하나는 에너지다
        - 처음에는 넷 다 에너지로 잘못 봤다. 종류 코드 4 를 에너지로 짐작했는데 실제로는 서포트였다
        - 에너지 카드 하나에 파일이 둘이다. 화면 전체를 덮는 것과 카드에 붙는 표시다
    - 옮기는 것
        - 5파일 3,356줄
    - 건드리지 않는 것
        - 카드가 아닌 다섯 파일 - 연출 종류, 연출 처리, 카드 이동, 스킬 재생, 자리 값. 카드별로 늘어나는 것이 아니다
        - 파일 안의 코드 - 위치와 경로만 바꾼다

2. 종류를 카드 데이터에서 확인한다
    - src/card/kind.ts 의 이름을 따른다. 숫자만 보고 뜻을 짐작하지 않는다
    - 서포트는 필드에 놓이고, 에너지는 유닛에게 붙는다

3. 한 카드에 파일이 여럿인 경우를 어떻게 둘지 정한다
    - 에너지 카드 하나가 화면 전체를 덮는 것과 카드에 붙는 표시 둘로 되어 있다
    - 하는 일이 다르지만 같은 카드의 것이라 한 폴더에 둔다. 그 카드를 고칠 때 둘 다 보게 된다

4. 옮기고 이관이 성립하는지 확인한다
    - 타입 검사와 의존 검사가 통과한다
    - 최상위에 카드 연출이 남지 않았는지 본다
    - 빈 폴더와 나갔다 들어오는 경로를 본다

5. 게임 동작이 달라지지 않았는지 확인한다
    - 넘쳐흐르는 사기, 망자의 늪, 레오닉의 부름
    - 차갑게 불타는 암흑 에너지를 붙였을 때 표시와 화면 효과

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 카드가 아닌 다섯 파일을 범위 밖으로 명시

2. 종류를 확인한다
    - [x] 카드 데이터에서 종류 값 확인
    - [x] 처음 판단이 틀렸던 것 기록

3. 한 카드에 파일이 여럿인 경우를 정한다
    - [x] 같은 카드면 한 폴더에 둔다

4. 옮기고 확인한다
    - [x] support 아래로 3파일, energy 아래로 2파일
    - [x] 부르는 곳 경로 수정
    - [x] 빈 폴더 삭제
    - [x] npx tsc --noEmit 통과
    - [x] npm run depcruise 통과
    - [x] 시나리오 빌드 통과

5. 동작 확인
    - [ ] 넘쳐흐르는 사기, 망자의 늪, 레오닉의 부름, 차갑게 불타는 암흑 에너지

# Issue

1. 종류를 숫자만 보고 짐작해 세 장을 틀렸다
    - 증상
        - 넘쳐흐르는 사기, 망자의 늪, 레오닉의 부름을 에너지 카드로 봤다. 실제로는 서포트다
    - 원인
        - 카드 데이터의 종류 칸이 숫자다. 4 를 에너지로 짐작했는데 서포트였다
        - src/card/kind.ts 에 이름이 적혀 있는데 찾아보지 않았다
    - 처리
        - 그 표를 확인하고 다시 갈랐다. 이동 계획표에도 그 표를 옮겨 적었다
        - **데이터의 코드 값은 뜻이 적힌 곳을 찾아 본다.** 짐작한 표는 맞아 보여도 틀린다

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 옮긴 파일 | 5개 3,356줄 | 코드는 그대로다 |
| 서포트 | 3파일 | 002, 020, 030 |
| 에너지 | 2파일 | 151 한 장에 파일이 둘 |
| 없어진 최상위 폴더 | 4개 | |
| 최상위에 남은 카드 연출 | 없음 | R2-39 부터 넷으로 다 옮겼다 |
| 타입 검사 | 오류 0 | |
| 의존 검사 | error 0, warn 6 | 기존 순환 그대로 |
| 동작 변경 | 없음 | 위치와 경로만 바꿨다 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | 종류를 카드 데이터에서 확인했다. 앞서 짐작으로 세 장을 틀린 적이 있어 이번에는 표를 봤다. |
| **잘 맞은 것** | 한 카드의 파일 둘을 한 폴더에 두었다. 하는 일은 다르지만 그 카드를 고칠 때 둘 다 보게 된다. |
| **미흡한 것** | 카드 연출 이관을 네 티켓으로 나눴는데, 부르는 곳이 확인용 화면 하나뿐이라 한 번에 해도 됐을 수 있다. 다만 종류가 다르면 나중에 다른 이유로 바뀐다. |
| **다음 작업에 넘길 것** | 카드가 아닌 다섯 파일. 연출 처리는 R2-44, 나머지는 자리가 이미 맞다 |
