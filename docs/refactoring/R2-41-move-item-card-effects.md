# [ETWGL-R2-41] 아이템 카드 연출 패키지 조정

선행: R2-40 │ 후행: R2-42 │ 산출물: src/battle/animation/card/item/ │ Type: Structure │ Domain: Battle Animation

# Success criteria

1. 왜 하는지와 어디까지 할지를 정한다
    - 문제
        - 아이템 카드 여섯 장의 연출이 최상위에 효과 이름으로 흩어져 있었다
        - 폴더 이름이 효과 이름이라 어느 카드의 것인지 알 수 없다. 낫이라는 이름을 보고 무기 공격으로 잘못 읽은 적이 있다
    - 목적
        - 아이템 카드 연출을 카드번호와 함께 한 서랍에 모은다
    - 기대 결과
        - 폴더를 열면 어느 카드의 것인지, 그 카드가 어떻게 쓰이는지가 이름에 드러난다
    - 옮기기 전에 확인한 것
        - 여섯 다 카드 데이터의 종류가 아이템이다. 상대 유닛에 떨어뜨려 쓰고 소모된다
        - 각 카드가 파일 하나씩이다. 서로 부르는 것이 없다
    - 옮기는 것
        - 6파일 5,419줄
    - 건드리지 않는 것
        - 서포트와 에너지 카드 연출 - 종류가 다르다. R2-42
        - 파일 안의 코드 - 위치와 경로만 바꾼다

2. 여섯이 정말 아이템인지 확인한다
    - 카드 데이터의 종류 값을 본다. 이름으로 짐작하지 않는다
    - 종류 값은 src/card/kind.ts 의 이름을 따른다

3. 옮긴다
    - 카드번호를 세 자리로 맞춰 폴더 이름에 넣는다. 번호순으로 정렬된다
    - 부르는 곳의 경로를 고친다

4. 이관이 성립하는지 확인한다
    - 타입 검사와 의존 검사가 통과한다
    - 빈 폴더와 나갔다 들어오는 경로를 본다

5. 게임 동작이 달라지지 않았는지 확인한다
    - 여섯 카드를 상대 유닛에 떨어뜨려 연출이 도는지

# To-do

1. 왜 하는지와 범위를 적는다
    - [x] 문제, 목적, 기대 결과 작성
    - [x] 서포트와 에너지를 범위 밖으로 명시

2. 종류를 확인한다
    - [x] 카드 데이터에서 종류 값 확인
    - [x] 카드번호 확인

3. 옮긴다
    - [x] item 아래로 6파일
    - [x] 부르는 곳 경로 수정
    - [x] 빈 폴더 삭제

4. 이관 확인
    - [x] npx tsc --noEmit 통과
    - [x] npm run depcruise 통과
    - [x] 나갔다 들어오는 경로 확인
    - [x] 시나리오 빌드 통과

5. 동작 확인
    - [x] 죽음의 낫, 에너지 번, 파멸의 계약, 시체 폭발, 사기 전환, 죽음의 대지

# Issue

1. 없음

# Review

| 항목 | 값 | 판단 |
| --- | --- | --- |
| 옮긴 파일 | 6개 5,419줄 | 코드는 그대로다 |
| 고친 참조 | 1파일 | 확인용 화면 하나가 여섯을 다 쓴다 |
| 없어진 최상위 폴더 | 6개 | |
| 타입 검사 | 오류 0 | |
| 의존 검사 | error 0, warn 6 | 기존 순환 그대로 |
| 동작 변경 | 없음 | 위치와 경로만 바꿨다 |

| 구분 | 내용 |
| --- | --- |
| **잘 맞은 것** | 카드번호를 폴더 이름에 넣었다. 배선이 카드번호로 갈리므로 번호로 찾으면 배선과 연출이 함께 나온다. |
| **잘 맞은 것** | 종류를 카드 데이터에서 확인했다. 이름만 보고 낫을 무기 공격으로 읽은 적이 있어, 이번에는 종류 값을 봤다. |
| **다음 작업에 넘길 것** | 서포트 세 장과 에너지 한 장. R2-42 |
