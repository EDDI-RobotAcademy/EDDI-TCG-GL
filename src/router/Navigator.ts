// 화면을 옮겨 달라고 부탁하는 창구다.
//
// 화면이 길 안내 전체를 알 필요가 없다. 쓰는 것은 [저 길로 가 달라] 하나뿐이다.
//
// 전에는 화면이 길 안내를 통째로 받았고, 길 안내는 화면 목록을 들여왔다. 그래서
// **화면 하나를 건드리면 화면 다섯이 다 딸려왔다** — 화면별로 쪼갤 덩이가 아예 없었다
// (R2-134). 이 창구만 받으면 그 고리가 끊긴다.
export interface Navigator {
    navigate(path: string): void;
}
